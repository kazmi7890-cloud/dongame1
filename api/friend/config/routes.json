{
  "routes": [
    {
      "method": "GET",
      "path": "/friends",
      "handler": "friend.find",
      "config": {
        "policies": []
      }
    },
    {
      "method": "GET",
      "path": "/friends/count",
      "handler": "friend.count",
      "config": {
        "policies": []
      }
    },
    {
      "method": "GET",
      "path": "/friends/:id",
      "handler": "friend.findOne",
      "config": {
        "policies": []
      }
    },
    {
      "method": "POST",
      "path": "/friends",
      "handler": "friend.create",
      "config": {
        "policies": []
      }
    },
    {
      "method": "PUT",
      "path": "/friends/:id",
      "handler": "friend.update",
      "config": {
        "policies": []
      }
    },
    {
      "method": "DELETE",
      "path": "/friends/:id",
      "handler": "friend.delete",
      "config": {
        "policies": []
      }
    }
  ]
}
